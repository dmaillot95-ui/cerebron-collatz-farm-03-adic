name: CEREBRON Farm 03 5min Scheduler

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

permissions:
  actions: write
  contents: read

concurrency:
  group: cerebron-farm03-scheduler
  cancel-in-progress: true

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch adic farm
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh workflow run adic-farm.yml --repo "${{ github.repository }}" --ref main
